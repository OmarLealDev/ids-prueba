#include <stdio.h>

int main(){
    int a,b,c,d;
    printf("Ingresa un numero de 4 digitos: ");
    scanf("%c %c %c %c", &a, &b, &c, &d);
    printf("El numero al reves es:  %c%c%c%c", d,c,b,a);

    return 0;
}